# Batch H — executor input list (BRIEF-FACING)

GENERATED by `batch-h-tools/derive-input-list.py` from the seat-facing argument
census. Do not hand-edit — the checker regenerates it and compares byte for byte.

Only the census's INPUT column survives. What each input reaches, and how the
incumbent source classes it, are separate columns and are dropped whole — so the
brief cannot carry an outcome label, whatever that label is called. Spellings are
expanded, so every one is invoked as its own capture.

A row that names a fixture property rather than an argv is a fixture fact to
construct, not an argument to pass.


## Surface: Top-level dispatcher (SC-012b, SC-014)

- `-h`
- `--help`
- `help`
- `version`
- `--version`
- `-V`

## Surface: `steward` (SC-013)

- `-h`
- `--help`
- `help`
- `--detach`
- `--no-attach`
- help with trailing args
- `--detach --attach`
- `--attach --detach`
- `--detach --detach`

## Surface: `state` (SC-211a)

- no args
- `working`
- `waiting-user`
- `done`
- `blocked` with a reason
- `blocked` with no reason
- unknown mode
- empty-string mode
- mode with a leading `-`
- extra words after a legal mode

## Surface: `goal` (SC-211b)

- no args
- one word
- several words
- `--clear` alone
- `--clear` + extra arg
- `--help`
- `-h`
- text that is only control bytes

## Surface: `memo` (SC-211c)

- no args
- `add <text>`
- `add` with no text
- `add --topic <t> <text>`
- `add --topic` with no topic and no text
- `add --topic <t>` with a topic but no text
- `read`
- `read --topic <t>`
- `read --topic` alone
- `read <extra>`
- `tail`
- `tail <n>`
- `tail <non-numeric>`
- `tail <n> <extra>`
- unknown subcommand
- any read/tail with no memo file

## Surface: `requests` (SC-211d, SC-212c)

- no args
- `mine`
- `inbox`
- `all`
- unknown mode
- `mine` where identity is undetectable
- `inbox` where identity is undetectable
- `all` where identity is undetectable
- extra args after the mode

## Surface: `say` (SC-211l)

- text via argv
- text via a pipe
- no args on a real TTY
- no args with redirected empty stdin
- whitespace-only text

## Surface: `peek` (SC-211e)

- no args
- a target spelling absent from the fixture
- an exact `alias:name` present in the fixture, no count
- non-numeric count
- negative count (`-5`)
- leading-plus count (`+5`)
- `0`
- count above 2000
- extra args after the count

## Surface: `agents` (SC-211f)

- no args
- `--all`
- any other argument
- `--all` with an unreadable session meta

## Surface: `focus` (SC-211g)

- no args
- a target spelling absent from the fixture
- an exact `alias:name` present in the fixture
- extra args

## Surface: `interrupt` (SC-211h)

- no args
- a target spelling absent from the fixture
- a present target, no message
- a present target + message, pane running a shell (H3)
- a present target + message, pane running the agent binary (H1)

## Surface: `spawn` (SC-211i)

- `AE_PATH` missing or not executable
- no args
- any args

## Surface: delegated `_cmd_spawn`

- an alias not defined in the config
- a session absent from meta
- a session named in meta but not running
- a complete meta/config/session fixture with `meta.lock` HELD BEYOND THE 5s WAIT
- a legal `alias` with no `:name`

## Surface: `retire` (SC-211j)

- `AE_PATH` missing or not executable
- no args
- any args

## Surface: delegated `_cmd_retire`

- a session absent from meta
- a `%pane-id` not in the session
- a bare name carried by two agents
- a name absent from the session
- the main agent's own reference
- a configured worker (not spawned)
- a reference absent from `agent.spawned.*`
- an agent reference present in `agent.spawned.*`
- extra arguments after the target

## Surface: `events-tail` (SC-211n)

- any argv at all
- events file absent
- events file present

## Surface: `_register-sid` (SC-211o)

- no slot argument
- a slot with no `launch_time.<slot>`
- non-numeric launch time
- candidate file older than launch time
- candidate with matching launch-id token
- candidate with a mismatched token
- no token match anywhere
- a malformed first-line id
- a missing first-line id
- yesterday's directory
- two candidates with EQUAL mtimes
- two candidates with DIFFERENT mtimes
- a candidate whose recorded cwd MATCHES the invoking cwd
- a candidate whose recorded cwd DIFFERS from the invoking cwd
- an explicitly named slot that is the invoking pane's
- an explicitly named slot that is NOT the invoking pane's

## Surface: `ae_resolve` (SC-211p)

- `%<pane-id>` (any)
- `@session:agent`, session exists, agent present
- `@session:agent`, session absent
- `@session` (no colon)
- `@:agent`
- `@session:`
- bare name, unique
- bare name, ambiguous
- alias-only, unique
- alias-only, ambiguous
- exact `alias:name` present
- name absent
- empty string
