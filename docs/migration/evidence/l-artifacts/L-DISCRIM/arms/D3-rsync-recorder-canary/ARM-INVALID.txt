ARM INVALID as a CANARY — and that is the finding.

ae valid-name transfer path did NOT reach rsync on this host: 0 rsync invocations recorded.
TWO independent gates precede any rsync invocation in the frozen code, and BOTH fail here:
  GATE 1  the SSH probe. The frozen call is a plain `ssh <target>`, and OpenSSH reads the
          config from the passwd-entry home, so a sandbox-only Host alias is unreachable.
          Reaching it would need the operator real ~/.ssh/config, root for port 22, or an
          argv-injecting ssh shim — the last of which is REFUSED by standing ruling.
  GATE 2  `rsync --protect-args --version`. This host only rsync is openrsync, which does
          not support it, so ae refuses BEFORE any rsync call even if GATE 1 passed.
          GATE 2 alone makes the ae path unreachable for ANY target on this host.

CONSEQUENCE, stated rather than worked around: the SC-814 rsync zero remains a zero from an
UNDEMONSTRATED recorder ON THE AE PATH. The recorder itself IS live — a direct rsync
invocation through the same shim recorded 2 invocation(s), rc 0 — but that is not the ae
path and does not license the SC-814 reading. No zero is reported here as an observation.
