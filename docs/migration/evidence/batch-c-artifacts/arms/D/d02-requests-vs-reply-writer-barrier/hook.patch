--- /private/tmp/claude-501/-Users-ckriech-projects-clemens33-ae-rust/347d2089-7268-421d-8188-8924e246bbf0/scratchpad/frozen/ae	2026-08-20 08:49:06
+++ /tmp/aecx/hooked/ae	2026-08-20 19:07:25
@@ -2679,6 +2679,23 @@
 }
 
 # List all ae sessions (tagged with AE_SESSION env var)
+# --- BATCH C INSTRUMENTATION: the ONE hook-only patch (cluster-plan global rule) ---
+# A hook is a NO-OP unless AE_HOOK names it: the guard is the first statement and
+# returns 0 before any side effect, so an INACTIVE hook writes nothing, prints nothing,
+# touches no file and cannot change an exit status. An ACTIVE hook only records that it
+# was reached and BLOCKS until the controller releases it — the controller, never the
+# hook, performs the named writer-shaped mutation.
+_ae_hook() {
+    [[ "${AE_HOOK:-}" == "$1" ]] || return 0
+    local _d="${AE_HOOK_DIR:-}"
+    [[ -n "$_d" ]] || return 0
+    printf '%s\t%s\t%s\n' "$1" "$(date -u +%FT%TZ)" "$$" >>"${_d}/hook.log"
+    : >"${_d}/${1}.reached"
+    while [[ ! -e "${_d}/${1}.release" ]]; do sleep 0.05; done
+    printf '%s\t%s\t%s\n' "${1}-RELEASED" "$(date -u +%FT%TZ)" "$$" >>"${_d}/hook.log"
+    return 0
+}
+
 list_ae_sessions() {
     local sessions="" _env
     while IFS= read -r name; do
@@ -3809,6 +3826,8 @@
         echo "ae next: no running session needs attention." >&2
         return 1
     fi
+
+    _ae_hook H_NEXT_SELECTED
 
     if ! $do_attach; then
         printf '%s  attn:%s  rank:%s  %s\n' "$best_name" "$best_reason" "$best_rank" "$best_agent"
@@ -3825,6 +3844,7 @@
         echo "ae next: '$best_name' disappeared before attach." >&2
         return 1
     fi
+    _ae_hook H_NEXT_RECHECKED
     # _ae_inside_tmux, not a bare $TMUX test (#29): with an inherited $TMUX,
     # display-message answers for the ORIGINAL pane's session, so a genuine jump
     # would be suppressed as "already in".
@@ -4159,6 +4179,7 @@
             sess_dir="$(_resolve_session_dir "$name")"
             local meta_blob=""
             [[ -n "$sess_dir" && -f "${sess_dir}/meta" ]] && meta_blob="$(<"${sess_dir}/meta")"
+            _ae_hook H_LIST_META_CAPTURED
             local origin mode workdir
             # One tmux round-trip for all three AE_* values, then meta as
             # fallback — parsed in-process instead of via echo|sed per field.
@@ -4528,6 +4549,7 @@
                 ;;
         esac
     done < <(_ae_tac "$file" 2>/dev/null || true)
+    _ae_hook H_REQUEST_SCAN_COMPLETE
     local i status summary
     local -a cands=()
     local cand c_actor c_target c_aslot c_tslot c_asess c_tsess c_sum
@@ -17719,7 +17741,7 @@
             ae_submit_pasted_message _capture_input_region _sgr_parse _input_region_prompt_seg _input_region_is_border _input_region_content_end _input_region_occupied _paste_still_staged _paste_input_busy _pane_recently_viewed \
             command_is_shell _agent_bin_for_pane _pane_agent_is_dead _pane_has_descendant_named \
             ae_meta_get ae_meta_set ae_meta_unset \
-            _ae_tac _ae_stat _ae_epoch _ae_json_first _ae_md5
+            _ae_tac _ae_stat _ae_epoch _ae_json_first _ae_md5 _ae_hook
     } >"${AE_META}/_lib.tmp.$$"
     chmod 0700 "${AE_META}/_lib.tmp.$$"
     mv "${AE_META}/_lib.tmp.$$" "${AE_META}/_lib"
